export interface catsType {
  name: string;
  ownerName: string;
}

export interface UsersType {
  id: string;
  name: string;
  username: string;
  email: string;
  password: string;
}

export interface ProductType {
  id: number;
  name: string;
  brand: string;
  price: number;
  discountPrice: number;
  stock: number;
}
